name: Deploy Sanity Studio

on:
  workflow_dispatch:

jobs:
codex/update-hosted-studio-schema-and-deploy-4tgcng
  deploy-studio:
    name: Deploy Sanity Studio
    runs-on: ubuntu-latest
    env:
      SANITY_AUTH_TOKEN: ${{ secrets.SANITY_AUTH_TOKEN }}
    defaults:
      run:
        working-directory: studio

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    env:
      SANITY_AUTH_TOKEN: ${{ secrets.SANITY_AUTH_TOKEN }}
codex/update-hosted-studio-schema-and-deploy-1yuwxb
    defaults:
      run:
        working-directory: studio
codex/update-hosted-studio-schema-and-deploy-lxp2uw
    defaults:
      run:
        working-directory: studio
        
main

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

codex/update-hosted-studio-schema-and-deploy-4tgcng
      - name: Check for merge conflict markers
        working-directory: .
        shell: bash
        run: |
          set -euo pipefail

codex/update-hosted-studio-schema-and-deploy-1yuwxb
      - name: Check for merge conflict markers
        working-directory: .

codex/update-hosted-studio-schema-and-deploy-lxp2uw
      - name: Check for merge conflict markers
main
        run: |
main
          if git grep -n '<<<<<<<'; then
            echo 'コンフリクトマーカー (<<<<<<<) が残っています。' >&2
            exit 1
          fi
          if git grep -n '>>>>>>>'; then
            echo 'コンフリクトマーカー (>>>>>>>) が残っています。' >&2
            exit 1
          fi

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
codex/update-hosted-studio-schema-and-deploy-4tgcng
          cache: npm
          cache-dependency-path: studio/package-lock.json

      - name: Install dependencies
        shell: bash
        run: |
          set -euo pipefail
          if [[ -f package-lock.json ]]; then

codex/update-hosted-studio-schema-and-deploy-1yuwxb
          cache: npm
          cache-dependency-path: studio/package-lock.json

main

      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then
main
            npm ci
          else
            npm install
          fi
codex/update-hosted-studio-schema-and-deploy-4tgcng

codex/update-hosted-studio-schema-and-deploy-1yuwxb
main

      - name: Verify Sanity auth token
        run: |
          if [ -z "${SANITY_AUTH_TOKEN}" ]; then
            echo 'SANITY_AUTH_TOKEN が設定されていません。' >&2
            exit 1
          fi

      - name: Deploy Sanity Studio
codex/update-hosted-studio-schema-and-deploy-4tgcng
        shell: bash
        run: |
          set -euo pipefail

        run: |
main
          npx --yes sanity@latest deploy \
            --project quljge22 \
            --dataset production \
            --non-interactive \
            --yes
codex/update-hosted-studio-schema-and-deploy-4tgcng


      - name: Deploy Sanity Studio
        run: |
          npx --yes sanity@latest deploy \
            --project quljge22 \
            --dataset production \
            --non-interactive \
            --yes

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Set up pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8

      - name: Install dependencies
        working-directory: studio
        run: pnpm install --frozen-lockfile

      - name: Deploy Sanity Studio
        working-directory: studio
        run: npx sanity deploy --project quljge22 --dataset production --yes
main
