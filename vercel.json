{
  "$schema": "https://openapi.vercel.sh/config/static/v3",
  "framework": "sveltekit",
  "installCommand": "pnpm install",
  "buildCommand": "rm -rf output && SKIP_SANITY=1 pnpm run build && mkdir -p output && cp -R .vercel/output/. ./output",
  "devCommand": "pnpm run dev",
  "outputDirectory": "output"
}
