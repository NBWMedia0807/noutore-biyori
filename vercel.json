{
  "$schema": "https://openapi.vercel.sh/config/static/v3",
  "framework": "sveltekit",
  "installCommand": "pnpm install",
  "buildCommand": "rm -rf output && pnpm run build && cp -R .vercel/output ./output",
  "devCommand": "pnpm run dev",
  "outputDirectory": "output"
}
